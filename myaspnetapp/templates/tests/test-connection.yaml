apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "myaspnetapp.fullname" . }}-test-connection"
  labels:
    {{- include "myaspnetapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "myaspnetapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
