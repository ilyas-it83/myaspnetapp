apiVersion: v1
kind: Service
metadata:
  name: {{ include "myaspnetapp.fullname" . }}
  labels:
    {{- include "myaspnetapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "myaspnetapp.selectorLabels" . | nindent 4 }}
